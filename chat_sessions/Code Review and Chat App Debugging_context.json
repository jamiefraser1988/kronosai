{"summarized_context": "\nReact app with chat functionality using `useState`, `useEffect`, and `useRef`. Imports `marked` for markdown parsing and `highlight.js` for code highlighting. Implements chat fetching, sending, saving, and deleting with API calls. Handles user input, chat history, and settings like theme, line height, and font size. Includes modal for settings and features like message expansion, scrolling, and copy-to-clipboard. Issues: redundant `setUserInput`, lack of user feedback on API\nMotion detection code examples using a camera in Python (OpenCV), JavaScript (OpenCV.js), and C# (AForge.NET):\n\n### Python (OpenCV)\n```python\nimport cv2\ncap = cv2.VideoCapture(0)\nfgbg = cv2.createBackgroundSubtractorMOG2(history=500, varThreshold=25, detectShadows=True)\nwhile True:\n    ret, frame = cap.read()\n    fgmask = fgbg.apply(frame)\n    contours,\n\"Show me C# examples\"  \nResponse: Motion detection using AForge.NET in C#.  \nCode includes:  \n- MainForm class with motion detection setup.\n- Form Designer Code for UI elements (`comboBoxDevices`, `btnStart`, `pictureBoxVideo`, `lblMotionLevel`)."}